{
  "name": "hubblerx-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd hubblers && npm install && npm run build",
    "build:all": "cd hubblers && npm install && npm run build && cd ../crm && npm install && npm run build",
    "build:backend": "cd hubblers && npm install && npx tsc -p server/tsconfig.json",
    "build:frontend": "cd hubblers && npm install && npm run build",
    "build:crm": "cd crm && npm install && npm run build",
    "start": "cd hubblers && node dist-server/index.js",
    "start:server": "cd hubblers && node dist-server/index.js"
  }
}
